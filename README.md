# The Wanderer

The continuation of the tbrg series

### Build Instructions

Requirements:
* CMake >= 2.8.4
* g++ (Clang and MSVC not tested)
* Make

At the root directory of the project:

```
$ mkdir build
$ cd build
$ cmake ..
$ make
```

A binary with the name of `the_wanderer` should appear. Run it!
