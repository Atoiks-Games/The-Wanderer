version: 1.0.{build}
image: Visual Studio 2015
configuration: Release
before_build:
- cmd: >-
    mkdir build

    cd build

    cmake ..
build:
  parallel: true
  verbosity: minimal
artifacts:
- path: build\release\the_wanderer.exe
  name: The Wanderer
